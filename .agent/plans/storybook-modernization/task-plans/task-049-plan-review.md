# task-049 plan review

## Planner: Iteration 1

2026-09-15.

Wrote `task-049.md` after measuring the corpus rather than after reading the entry's expectations of
it.

Critique of iteration 1:

- The plan produced an hours-per-screen figure by dividing the phase's estimate by its screens. That
  is the estimate coming back out of the instrument that was fed it, and it would then have been used
  to estimate phase 7. Replaced with what was actually measured, and an explicit statement of what was
  not.
- The plan asserted "no story constructs a store" in prose. It is a property of 29 files that will
  become 49, and it is mechanically decidable from the imports.
- The plan sampled a few story files for duplicated overrides. Read across all 29 instead; the answer
  is only useful if it is exhaustive, since one duplicated pair in an unread file is the case that
  matters.

Scope guard: no new screens, no changes under `source/`.

Outcome: `approved`.
