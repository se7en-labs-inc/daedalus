# task-033 plan review

## Planner: Iteration 1

2026-09-15.

Wrote `task-033.md` after grepping the repository rather than after reading the task entry, which
expects two files still to convert.

Critique of iteration 1:

- The plan carried the entry's expectation of a sweep. There is nothing to sweep: all 17 sites went
  with their files' knobs. What is left is the shim and the list.
- The plan proposed writing the exception rule into the Storybook skill document, following
  `task-033.targetPaths`. `task-026` already recorded the rule in the phase's reference and named
  `task-060` as the skill document's owner, so doing both creates two sources for one rule, which is
  what the entry's own note warns against. Removed from scope and recorded as a correction.
- The plan listed the exceptions as a count. Rewritten to state the reasoning, including the two sites
  that looked like exceptions and are not, because a list saying "none" teaches nothing.
- Added `jest` to the verification, since the file being deleted is a module other modules imported.

Scope guard: one deletion; no story file changes; no skill document changes.

Outcome: `approved`.
